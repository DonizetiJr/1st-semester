#include <bits/stdc++.h>

using namespace std;

const int N = 1000001;
char s[N];

bool isVowel(char c) {
	return (c == 'a' or c == 'e' or c == 'i'
			or c == 'o' or c == 'u');
}

int main(void) {

	scanf("%s", s);
	int len = strlen(s);

	printf("%c", s[0]);
	for(int i = 0; i < len; ++i) {
		if() {
			//entra se s[i] for vogal
			if() {
				//e s[i-1] nao for
			}
		} else {
			//entra se s[i] nao for vogal
		}
	}
}